import 'package:flutter/material.dart';

class AppTheme {
  static const Color themeColor = Colors.deepPurple;
  static const Color loginColor =  const Color(0xFF6C5CE7);

  static const Color lightBackground = Color(0xFFF7F7F9);
  static const Color dangerColor = Colors.red;
  static const Color successColor = Colors.green;

// You can also add text styles here if you want later.
}
